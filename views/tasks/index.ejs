<% include ../partials/header.ejs %>
    <% tasks.forEach(task => { %>
        <h2>
            <%= task.subject %>
                <%= task.content %>
        </h2>
        <p>
<%= task.due_date %>
    </p>

            <% }); %>
                <% include ../partials/footer.ejs %>

